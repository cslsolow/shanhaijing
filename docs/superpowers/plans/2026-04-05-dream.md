# Dream 功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 `dream` 命令，每天深夜从知识库中选取高摩擦概念组合，多轮演化生成梦境文章，存入 `dream/` 目录。

**Architecture:** `shanhaijing/dream.py` 负责全部逻辑（摩擦度计算、概念选取、多轮生成、cron 注册）。`main.py` 新增 `dream` 子命令。每轮生成后 sleep 指定时间，再用下一个概念改写上一版，最终生成演化链。

**Tech Stack:** Python 3.11+, `crontab` (系统 cron via subprocess), `llm.call()` (已有), `config.load/save()` (已有)

---

## 文件映射

| 操作 | 文件 |
|------|------|
| 新建 | `shanhaijing/dream.py` |
| 新建 | `tests/test_dream.py` |
| 修改 | `shanhaijing/config.py` — 新增 dream 配置字段 |
| 修改 | `main.py` — 新增 dream 子命令 |
| 修改 | `skill/SKILL.md` — 新增 `/shj dream` 条目 |

---

## Task 1: 新增 dream 配置字段

**Files:**
- Modify: `shanhaijing/config.py`
- Test: `tests/test_dream.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_dream.py
import json, tempfile, os
from pathlib import Path

def test_dream_config_defaults():
    with tempfile.TemporaryDirectory() as d:
        from shanhaijing import config
        cfg = config.load(d)
        assert cfg["dream_schedule"] == "0 23 * * *"
        assert cfg["dream_interval_min"] == 45
        assert cfg["dream_token_budget"] == 30000
        assert cfg["dream_concepts_count"] == 7
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/silinchen/shanhaijing
python -m pytest tests/test_dream.py::test_dream_config_defaults -v
```
Expected: FAIL — KeyError 或 AssertionError

- [ ] **Step 3: 在 config.py 的 DEFAULTS 里新增字段**

在 `shanhaijing/config.py` 的 `DEFAULTS` dict 末尾（`"zotero_collections": []` 后面）加：

```python
    # Dream
    "dream_schedule": "0 23 * * *",
    "dream_interval_min": 45,
    "dream_token_budget": 30000,
    "dream_concepts_count": 7,
```

同时在 `_FIELDS` 末尾把这四个字段加进去，方法是在 `_SYNC_FIELDS` 后面加：

```python
_DREAM_FIELDS = (
    "dream_schedule", "dream_interval_min",
    "dream_token_budget", "dream_concepts_count",
)
_FIELDS = _LLM_FIELDS + _SYNC_FIELDS + _DREAM_FIELDS
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/test_dream.py::test_dream_config_defaults -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shanhaijing/config.py tests/test_dream.py
git commit -m "feat: add dream config fields"
```

---

## Task 2: 摩擦度计算

**Files:**
- Create: `shanhaijing/dream.py`
- Test: `tests/test_dream.py`

- [ ] **Step 1: 写失败测试**

```python
# 在 tests/test_dream.py 追加

import tempfile
from pathlib import Path

def _make_kb(tmp):
    kb = Path(tmp)
    (kb / "wiki/concepts").mkdir(parents=True)
    (kb / "wiki/summaries").mkdir(parents=True)
    # concept A: 在 summary1 出现
    (kb / "wiki/summaries/s1.md").write_text("links: [[concept-a]] [[concept-b]]")
    # concept B: 在 summary1 出现（和 A 共现）
    # concept C: 在 summary2 出现（和 A/B 不共现）
    (kb / "wiki/summaries/s2.md").write_text("links: [[concept-c]]")
    (kb / "wiki/concepts/concept-a.md").write_text("---\ntitle: A\ningested: 2020-01-01\n---")
    (kb / "wiki/concepts/concept-b.md").write_text("---\ntitle: B\ningested: 2021-01-01\n---")
    (kb / "wiki/concepts/concept-c.md").write_text("---\ntitle: C\ningested: 2022-01-01\n---")
    return kb

def test_friction_scores():
    with tempfile.TemporaryDirectory() as tmp:
        from shanhaijing.dream import compute_friction
        kb = _make_kb(tmp)
        scores = compute_friction(kb)
        # concept-a と concept-c は共現なし → 高摩擦
        assert scores[("concept-a", "concept-c")] == 0
        # concept-a と concept-b は共現あり → 低摩擦
        assert scores[("concept-a", "concept-b")] >= 1
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python -m pytest tests/test_dream.py::test_friction_scores -v
```
Expected: FAIL — ImportError (dream.py 不存在)

- [ ] **Step 3: 创建 dream.py，实现 compute_friction**

创建 `shanhaijing/dream.py`：

```python
"""Dream workflow — multi-round concept dreaming with friction-based selection."""

import random
import re
import time
from datetime import date
from pathlib import Path

from . import config as cfg_mod
from . import llm

ANGLES = [
    ("contrarian", "找出知识库里最确信的结论，然后用知识库本身的证据反驳它。夸大矛盾，走向极端。"),
    ("analogy",    "把两个表面毫不相关的概念强行类比，找到荒谬但在逻辑上成立的共同点。越意外越好。"),
    ("hypothesis", "提出3个知识库里隐含但从未被明说的假设，每个都要有点惊世骇俗。"),
    ("genealogy",  "编一段虚构但内在合理的思想史：这些概念如果早200年相遇，会发生什么？"),
    ("critique",   "化身知识库主流观点的死对头，用知识库外部的视角猛烈批判它的盲点。"),
]


def _parse_frontmatter_date(text: str) -> str:
    for line in text.splitlines():
        line = line.strip()
        if line.startswith("ingested:"):
            return line.split(":", 1)[1].strip()
    return "1970-01-01"


def compute_friction(kb_path) -> dict:
    """Return dict of (concept_a, concept_b) -> co-occurrence count (lower = higher friction)."""
    kb = Path(kb_path)
    summaries_dir = kb / "wiki" / "summaries"
    concepts_dir = kb / "wiki" / "concepts"

    concept_slugs = {p.stem for p in concepts_dir.glob("*.md")}
    co_occurrence = {}

    for summary_path in summaries_dir.glob("*.md"):
        text = summary_path.read_text(encoding="utf-8", errors="ignore")
        found = {m for m in re.findall(r"\[\[([^\]]+)\]\]", text) if m in concept_slugs}
        found_list = sorted(found)
        for i, a in enumerate(found_list):
            for b in found_list[i + 1:]:
                key = (a, b)
                co_occurrence[key] = co_occurrence.get(key, 0) + 1

    # ensure all pairs exist (missing = 0 co-occurrences)
    slugs = sorted(concept_slugs)
    for i, a in enumerate(slugs):
        for b in slugs[i + 1:]:
            key = (a, b)
            if key not in co_occurrence:
                co_occurrence[key] = 0

    return co_occurrence


def select_concepts(kb_path, n: int, seed: int) -> list:
    """Select n high-friction concepts sorted by source date."""
    rng = random.Random(seed)
    kb = Path(kb_path)
    concepts_dir = kb / "wiki" / "concepts"

    friction = compute_friction(kb_path)
    # pick pairs with lowest co-occurrence (highest friction)
    sorted_pairs = sorted(friction.items(), key=lambda x: x[1])
    # collect unique concepts from top-friction pairs
    selected = []
    seen = set()
    for (a, b), _ in sorted_pairs:
        for c in (a, b):
            if c not in seen:
                seen.add(c)
                selected.append(c)
        if len(selected) >= n:
            break

    # fallback: pad with random if not enough
    all_slugs = [p.stem for p in concepts_dir.glob("*.md")]
    rng.shuffle(all_slugs)
    for s in all_slugs:
        if s not in seen and len(selected) < n:
            selected.append(s)

    # sort by ingested date
    def get_date(slug):
        p = concepts_dir / f"{slug}.md"
        if p.exists():
            return _parse_frontmatter_date(p.read_text(encoding="utf-8", errors="ignore"))
        return "1970-01-01"

    selected = selected[:n]
    selected.sort(key=get_date)
    return selected
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/test_dream.py::test_friction_scores -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shanhaijing/dream.py tests/test_dream.py
git commit -m "feat: dream — friction computation and concept selection"
```

---

## Task 3: 单轮梦境生成

**Files:**
- Modify: `shanhaijing/dream.py`
- Test: `tests/test_dream.py`

- [ ] **Step 1: 写失败测试（mock LLM）**

```python
# 在 tests/test_dream.py 追加

from unittest.mock import patch

def test_generate_round_writes_file():
    with tempfile.TemporaryDirectory() as tmp:
        kb = _make_kb(Path(tmp))
        (kb / "dream").mkdir()

        with patch("shanhaijing.dream.llm.call", return_value="# 梦境\n\n测试内容"):
            from shanhaijing.dream import generate_round
            out_path = generate_round(
                kb_path=kb,
                cfg={"provider": "anthropic", "model": "test"},
                date_str="2026-04-06",
                round_num=1,
                concept="concept-a",
                angle=("contrarian", "反驳最确信的结论"),
                seed="abcd",
                prev_text=None,
                concepts_so_far=[],
                forgotten=[],
                token_budget=30000,
            )
            assert out_path.exists()
            content = out_path.read_text()
            assert "round: 1" in content
            assert "concept-a" in content
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python -m pytest tests/test_dream.py::test_generate_round_writes_file -v
```
Expected: FAIL — ImportError (generate_round 不存在)

- [ ] **Step 3: 在 dream.py 实现 generate_round**

在 `shanhaijing/dream.py` 的 `select_concepts` 函数后面追加：

```python
ROUND_SYSTEM = """\
你是一个深夜做梦的研究者。你的知识库喂养了你，现在你在睡梦中用以下角度思考这些概念。

角度：{angle_desc}

规则：
- 写散文，不是论文。有观点，有情绪，有夸张。
- 如果是后续轮次，你必须：(a) 融入新概念，扭曲已有论点；(b) 随机「遗忘」1-2个前一版的论点（在 forgotten 列表里标出）
- 时间轴叙事：概念按发现时间从旧到新，思想像在演化
- 长度：400-800字
- 输出 ONLY markdown 正文，不含 frontmatter\
"""


def _estimate_tokens(text: str) -> int:
    return len(text) // 3


def generate_round(
    kb_path,
    cfg: dict,
    date_str: str,
    round_num: int,
    concept: str,
    angle: tuple,
    seed: str,
    prev_text: str | None,
    concepts_so_far: list,
    forgotten: list,
    token_budget: int,
) -> Path:
    kb = Path(kb_path)
    dream_dir = kb / "dream" / date_str
    dream_dir.mkdir(parents=True, exist_ok=True)

    angle_slug, angle_desc = angle

    # Read concept article
    concept_path = kb / "wiki" / "concepts" / f"{concept}.md"
    concept_text = concept_path.read_text(encoding="utf-8") if concept_path.exists() else f"概念：{concept}"

    # Read related summaries up to token budget
    summaries_dir = kb / "wiki" / "summaries"
    summaries_text = ""
    used_tokens = _estimate_tokens(concept_text) + (
        _estimate_tokens(prev_text) if prev_text else 0
    )
    for sp in summaries_dir.glob("*.md"):
        txt = sp.read_text(encoding="utf-8", errors="ignore")
        if concept in txt:
            t = _estimate_tokens(txt)
            if used_tokens + t < token_budget - 5000:
                summaries_text += f"\n---\n{txt}"
                used_tokens += t

    # Build user prompt
    user_parts = [f"## 当前概念：{concept}\n\n{concept_text}"]
    if summaries_text:
        user_parts.append(f"## 相关原文摘要\n{summaries_text}")
    if prev_text:
        user_parts.append(f"## 上一版梦境（第{round_num-1}轮）\n\n{prev_text}")
    if concepts_so_far:
        user_parts.append(f"## 已融入的概念\n{', '.join(concepts_so_far)}")

    user_prompt = "\n\n".join(user_parts)
    system = ROUND_SYSTEM.format(angle_desc=angle_desc)

    body = llm.call(cfg, system, user_prompt, max_tokens=1200)

    # Parse forgotten from LLM output (look for <!-- forgotten: ... --> marker)
    new_forgotten = forgotten[:]
    forgotten_match = re.search(r"<!--\s*forgotten:\s*(.+?)\s*-->", body)
    if forgotten_match:
        new_forgotten.append(forgotten_match.group(1).strip())
        body = re.sub(r"<!--\s*forgotten:.+?-->", "", body).strip()

    # Build frontmatter
    all_concepts = concepts_so_far + [concept]
    fm_lines = [
        "---",
        f"date: {date_str}",
        f"round: {round_num}",
        f"angle: {angle_slug}",
        f"seed: {seed}",
        f"concept_added: {concept}",
        f"concepts_so_far: [{', '.join(all_concepts)}]",
    ]
    if new_forgotten:
        forgotten_yaml = ", ".join(f'"{f}"' for f in new_forgotten)
        fm_lines.append(f"forgotten: [{forgotten_yaml}]")
    fm_lines.append("---")
    fm = "\n".join(fm_lines)

    filename = f"v{round_num}-{concept}.md" if round_num > 1 else f"v{round_num}-{concept}.md"
    out_path = dream_dir / filename
    out_path.write_text(f"{fm}\n\n{body}\n", encoding="utf-8")

    return out_path
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/test_dream.py::test_generate_round_writes_file -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shanhaijing/dream.py tests/test_dream.py
git commit -m "feat: dream — single-round generation with frontmatter"
```

---

## Task 4: 多轮演化主流程 + final.md

**Files:**
- Modify: `shanhaijing/dream.py`
- Test: `tests/test_dream.py`

- [ ] **Step 1: 写失败测试**

```python
# 在 tests/test_dream.py 追加

def test_run_dream_creates_final(tmp_path):
    kb = _make_kb(tmp_path)
    cfg = {"provider": "anthropic", "model": "test",
           "dream_interval_min": 0, "dream_token_budget": 30000,
           "dream_concepts_count": 3}

    with patch("shanhaijing.dream.llm.call", return_value="# Dream\n\n内容"):
        from shanhaijing.dream import run
        result = run(kb, cfg, dry_run=True)
        assert result["rounds"] >= 1
        final = tmp_path / "dream" / result["date"] / "final.md"
        assert final.exists()
        assert "## 演化记录" in final.read_text()
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python -m pytest tests/test_dream.py::test_run_dream_creates_final -v
```
Expected: FAIL — ImportError (run 不存在)

- [ ] **Step 3: 在 dream.py 实现 run()**

在 `shanhaijing/dream.py` 末尾追加：

```python
def run(kb_path, cfg: dict = None, dry_run: bool = False) -> dict:
    kb = Path(kb_path)
    if cfg is None:
        cfg = cfg_mod.load(kb_path)

    today = date.today().isoformat()
    seed_int = random.randint(0, 0xFFFF)
    seed_hex = f"{seed_int:04x}"

    n = cfg.get("dream_concepts_count", 7)
    interval_min = 0 if dry_run else cfg.get("dream_interval_min", 45)
    token_budget = cfg.get("dream_token_budget", 30000)

    concepts = select_concepts(kb_path, n, seed_int)
    if not concepts:
        return {"error": "No concepts found in wiki/concepts/"}

    angle = random.Random(seed_int).choice(ANGLES)

    print(f"Dream {today} | seed={seed_hex} | angle={angle[0]} | concepts={len(concepts)}")

    prev_text = None
    concepts_so_far = []
    forgotten = []
    round_paths = []

    for i, concept in enumerate(concepts):
        round_num = i + 1
        print(f"  Round {round_num}/{len(concepts)}: +{concept}")

        out_path = generate_round(
            kb_path=kb,
            cfg=cfg,
            date_str=today,
            round_num=round_num,
            concept=concept,
            angle=angle,
            seed=seed_hex,
            prev_text=prev_text,
            concepts_so_far=concepts_so_far,
            forgotten=forgotten,
            token_budget=token_budget,
        )
        round_paths.append(out_path)
        prev_text = out_path.read_text(encoding="utf-8")
        # strip frontmatter for next round's prev_text
        if prev_text.startswith("---"):
            parts = prev_text.split("---", 2)
            if len(parts) >= 3:
                prev_text = parts[2].strip()

        concepts_so_far.append(concept)

        if i < len(concepts) - 1 and interval_min > 0:
            print(f"  Sleeping {interval_min}min before next round...")
            time.sleep(interval_min * 60)

    # Write final.md
    dream_dir = kb / "dream" / today
    evolution_log = "\n".join(
        f"- Round {j+1}: +{c}" for j, c in enumerate(concepts)
    )
    final_content = f"""---
date: {today}
angle: {angle[0]}
seed: {seed_hex}
concepts: [{', '.join(concepts)}]
rounds: {len(concepts)}
---

{prev_text}

## 演化记录

{evolution_log}
"""
    (dream_dir / "final.md").write_text(final_content, encoding="utf-8")
    print(f"  -> dream/{today}/final.md")

    return {"date": today, "rounds": len(concepts), "angle": angle[0], "seed": seed_hex}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/test_dream.py::test_run_dream_creates_final -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shanhaijing/dream.py tests/test_dream.py
git commit -m "feat: dream — multi-round evolution loop and final.md"
```

---

## Task 5: cron 注册/取消

**Files:**
- Modify: `shanhaijing/dream.py`
- Test: `tests/test_dream.py`

- [ ] **Step 1: 写失败测试**

```python
# 在 tests/test_dream.py 追加

def test_schedule_adds_crontab(tmp_path):
    with patch("shanhaijing.dream.subprocess.run") as mock_run:
        mock_run.return_value.stdout = ""
        mock_run.return_value.returncode = 0
        from shanhaijing.dream import schedule, unschedule
        schedule(str(tmp_path), "0 23 * * *")
        # subprocess.run が呼ばれたことを確認
        assert mock_run.called

def test_unschedule_removes_crontab(tmp_path):
    existing = f"0 23 * * * uv run main.py dream {tmp_path}\n"
    with patch("shanhaijing.dream.subprocess.run") as mock_run:
        mock_run.return_value.stdout = existing
        mock_run.return_value.returncode = 0
        from shanhaijing.dream import unschedule
        unschedule(str(tmp_path))
        assert mock_run.called
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python -m pytest tests/test_dream.py::test_schedule_adds_crontab tests/test_dream.py::test_unschedule_removes_crontab -v
```
Expected: FAIL

- [ ] **Step 3: 在 dream.py 实现 schedule/unschedule**

在 `shanhaijing/dream.py` 的 import 区加 `import subprocess`，然后在文件末尾追加：

```python
import subprocess


def _cron_tag(kb_path: str) -> str:
    return f"shj-dream:{kb_path}"


def schedule(kb_path: str, cron_expr: str = "0 23 * * *"):
    kb = str(Path(kb_path).resolve())
    tag = _cron_tag(kb)
    # read existing crontab
    result = subprocess.run(["crontab", "-l"], capture_output=True, text=True)
    existing = result.stdout if result.returncode == 0 else ""
    # remove any existing shj-dream entry for this kb
    lines = [l for l in existing.splitlines() if tag not in l]
    # add new entry
    cmd = f"cd {Path(__file__).parent.parent} && uv run main.py dream {kb}"
    lines.append(f"{cron_expr} {cmd} # {tag}")
    new_crontab = "\n".join(lines) + "\n"
    subprocess.run(["crontab", "-"], input=new_crontab, text=True, check=True)
    print(f"Scheduled: {cron_expr} → dream {kb}")


def unschedule(kb_path: str):
    kb = str(Path(kb_path).resolve())
    tag = _cron_tag(kb)
    result = subprocess.run(["crontab", "-l"], capture_output=True, text=True)
    if result.returncode != 0:
        print("No crontab found.")
        return
    lines = [l for l in result.stdout.splitlines() if tag not in l]
    new_crontab = "\n".join(lines) + "\n"
    subprocess.run(["crontab", "-"], input=new_crontab, text=True, check=True)
    print(f"Unscheduled dream for {kb}")
```

注意：把 `import subprocess` 移到文件顶部的 import 区（和其他 import 放一起）。

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/test_dream.py::test_schedule_adds_crontab tests/test_dream.py::test_unschedule_removes_crontab -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shanhaijing/dream.py tests/test_dream.py
git commit -m "feat: dream — cron schedule/unschedule"
```

---

## Task 6: main.py 新增 dream 子命令

**Files:**
- Modify: `main.py`
- Test: `tests/test_dream.py`

- [ ] **Step 1: 写失败测试**

```python
# 在 tests/test_dream.py 追加

def test_cli_dream_now(tmp_path):
    _make_kb(tmp_path)
    with patch("shanhaijing.dream.llm.call", return_value="# Dream\n\n内容"):
        import subprocess, sys
        result = subprocess.run(
            [sys.executable, "main.py", "dream", str(tmp_path), "--now"],
            capture_output=True, text=True,
            cwd="/Users/silinchen/shanhaijing"
        )
        assert result.returncode == 0, result.stderr
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python -m pytest tests/test_dream.py::test_cli_dream_now -v
```
Expected: FAIL — `argument command: invalid choice: 'dream'`

- [ ] **Step 3: 在 main.py 新增 dream 命令**

在 `main.py` 的 `cmd_distill` 函数后面，`def main()` 前面，插入：

```python
def cmd_dream(args):
    from shanhaijing import dream
    from shanhaijing import config as cfg_mod
    kb = os.path.abspath(args.kb or ".")
    cfg = cfg_mod.load(kb)

    if args.schedule:
        cron = cfg.get("dream_schedule", "0 23 * * *")
        dream.schedule(kb, cron)
    elif args.unschedule:
        dream.unschedule(kb)
    else:
        result = dream.run(kb, cfg)
        if "error" in result:
            print(f"Error: {result['error']}", file=sys.stderr)
            sys.exit(1)
```

在 `main()` 的 `sub = parser.add_subparsers(...)` 后面，在 `args = parser.parse_args()` 前面，插入：

```python
    p_dream = sub.add_parser("dream", help="Multi-round concept dreaming")
    p_dream.add_argument("kb", nargs="?", default="")
    p_dream.add_argument("--now", action="store_true", help="Run immediately (default)")
    p_dream.add_argument("--schedule", action="store_true", help="Register cron job")
    p_dream.add_argument("--unschedule", action="store_true", help="Remove cron job")
```

在 `main()` 的命令分发 dict 里加：

```python
        "dream": cmd_dream,
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/test_dream.py::test_cli_dream_now -v
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add main.py tests/test_dream.py
git commit -m "feat: add dream subcommand to CLI"
```

---

## Task 7: skill/SKILL.md 新增 /shj dream

**Files:**
- Modify: `skill/SKILL.md`

- [ ] **Step 1: 在 SKILL.md 的 Sub-commands 表格里加一行**

找到表格：

```markdown
| `lint`    | `/shj lint [path]`   | Health check: broken links, orphans, gaps |
```

在这行后面加：

```markdown
| `dream`   | `/shj dream [path]`  | Multi-round dreaming across high-friction concepts; stores in `dream/` |
```

- [ ] **Step 2: 在 SKILL.md 末尾加 Dream Workflow 章节**

在 `## Non-goals and Constraints` 前面插入：

```markdown
## Dream Workflow

`/shj dream [path]` runs: `uv run main.py dream <path>`

Python handles the full loop — concept selection, LLM calls, sleep intervals, and file writing. Claude Code is only the trigger.

Output lands in `<base>/dream/YYYY-MM-DD/`:
- `v1-<concept>.md` through `vN-<concept>.md` — per-round evolution
- `final.md` — last version with evolution log

To schedule nightly dreams: `/shj dream --schedule` (registers cron at 23:00 local time).
```

- [ ] **Step 3: Commit**

```bash
git add skill/SKILL.md
git commit -m "docs: add dream workflow to skill/SKILL.md"
```

---

## Task 8: 集成测试 + 全量测试

- [ ] **Step 1: 运行全部 dream 测试**

```bash
python -m pytest tests/test_dream.py -v
```
Expected: 全部 PASS

- [ ] **Step 2: 确认 dream 目录不进 git**

```bash
grep "dream" /Users/silinchen/shanhaijing/.gitignore
```
Expected: 有 `dream/` 或 `*/dream/` 这一行。如果没有，在 `.gitignore` 加：

```
*/dream/
```

- [ ] **Step 3: 手动冒烟测试（用真实 KB）**

```bash
cd /Users/silinchen/shanhaijing
uv run main.py dream ./public --now
```
Expected: 打印轮次进度，`public/dream/YYYY-MM-DD/` 下有 v1-*.md 和 final.md

- [ ] **Step 4: Final commit**

```bash
git add .gitignore
git commit -m "chore: gitignore dream output directories"
```
